# Results

This directory stores derived outputs generated from checked-in sample reports.

Regenerate with:

```bash
python tools/generate_report.py
```

Current results are AVD-limited and sample-driven. They validate the parser, normalizer, diff engine, schema, and report flow. They do not claim hardware-backed physical-device trust behavior.
