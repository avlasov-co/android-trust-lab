# Writable-System AVD Sample

This directory contains a synthetic AVD-limited sample for `E03_writable_system_avd`.

The sample models an overlay-backed writable `/system` mount. It is useful for analyzer and diff validation only. It does not prove physical-device boot-chain behavior, AVB behavior, TEE behavior, or OEM-specific behavior.
