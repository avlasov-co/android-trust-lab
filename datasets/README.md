# Datasets

Dataset samples are non-authoritative. Emulator samples validate the pipeline. Physical-device claims require physical-device artifacts.

Raw artifacts should be preserved where safe. Do not commit private identifiers.
